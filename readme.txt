Entry for Kaggle's digit recognizer competition.

Implements a neural network with 2 hidden layers, each with 800 hidden units. 500 epochs results in ~94% accuracy.

Currently uses logistic regression.  I would like to implement softmax.
